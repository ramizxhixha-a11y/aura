# PASSATION-AURA8 — 17/09/2026 — token `20260917b` — A13 MOTEUR DE SORTIE UNIQUE (niveaux ATR des bras A/B exécutés, breakeven réel) · école `20260917a` · push direct ACTIF · passation versionnée

## Démarrage de session — Rams colle le PAT, rien d'autre (ce fichier est lu depuis le dépôt)
1. Cloner : `git clone https://x-access-token:PAT@github.com/ramizxhixha-a11y/aura.git` puis aussitôt `git remote set-url origin https://github.com/ramizxhixha-a11y/aura.git` — le PAT ne reste ni dans `.git/config`, ni dans un fichier, ni en mémoire.
2. Lire CE fichier, puis `node banc-all.js` sur l'état brut du dépôt → « VERDICT : LIVRABLE » attendu AVANT toute modification ; sinon la mission devient « comprendre pourquoi ».
3. Une seule mission par session, nommée avant la première modification. Aucun fichier « en plus ». Pas d'hypothèse corrigée sans sonde qui la nomme.
4. Fin : `node banc-all.js` LIVRABLE → **un seul commit** = fichiers modifiés + cette passation mise à jour (1re ligne au token courant ; banc-all BLOQUE si elle manque ou si le token diverge) → `git push https://x-access-token:PAT@github.com/ramizxhixha-a11y/aura.git main` → `curl` raw : `DOC_V = '<token>'` → Pages rebâtit (< 1 min) → Rams relance l'app.
5. Auteur des commits : `Rams (via Claude) <226786946+ramizxhixha-a11y@users.noreply.github.com>`. Message : `<token> · <mission> · banc-all LIVRABLE`.
6. PAT fine-grained, dépôt `aura` seul, Contents Read/Write, 30 j — celui du 12/09 expire le **12/10/2026**. Ancien PAT classique : à révoquer (Rams).
7. Au chat : court. **Lecture intégrale = dans le bac à sable** (grep de tous les appelants/lecteurs + bancs sur le texte livré), au chat seulement les fonctions touchées. C'est ce qui a saturé les sessions précédentes (20 000 lignes lues au chat par mission) — plus jamais. Le filet reste `banc-all` (collisions globales, syntaxe, HTML, passation), pas la lecture.

## Ce commit — A13 MOTEUR DE SORTIE UNIQUE — token `20260917b` — Rams « Go A13 » — touche l'argent (sorties EV)
**Avant** : trois sorties qui ne se parlaient pas pour une position bot — le % de conviction (`_tpPct/_slPct`, le seul qui fermait), les niveaux `pos.sl/pos.tp` ATR × multiplicateurs des bras A/B (09d1, posés une fois à l'ouverture) jamais comparés au prix, et un breakeven qui déplaçait `pos.sl`… que personne ne lisait : à +45 % de l'objectif la position n'était pas protégée. L'A/B mesurait du vide.
**Règle unique** (`_botExitSweep`, 10f, à chaque tick sur `ps.price`) : quand la position a ses niveaux (EV : dès le tick suivant l'ouverture) — **SL exécuté au niveau, TP exécuté au niveau** (sans attente de 5 cycles : le TP ATR est l'objectif de l'A/B), **breakeven exécuté** : à 45 % du chemin entrée → TP, `pos.sl` passe à l'entrée ± 0,1 % (`_beAt`). Sans niveaux (AA, tick d'ouverture) : repli % conviction, sémantique 1b-a inchangée. `_lossCapSweep` reste le filet absolu (règle 06/07). Résolution : Signal inversé / timeout inchangés. Positions manuelles : moteur 07 inchangé.
**Conséquence déclarée** : les sorties EV vont changer de forme — TP ATR ≈ 1,2-1,8 ATR (bras) au lieu de 2,7 % de conviction, SL ATR 2 au lieu de 0,9 % : positions fermées plus tôt en profit, plus tard en perte selon la volatilité. C'est le but : pour la première fois l'A/B compare deux règles de sortie qui existent. Le journal montre « TP 103.1200 » / « SL 98.0000 (breakeven) » au lieu de « TP +2,7 % ».
**Fichiers** : 10f, HTML → `20260917b` (79). `banc-verite-donnees.js` **25/25** (D14 : SL/TP au niveau, breakeven exécuté, short symétrique, repli % conservé). Pins : net-expectancy (10f), phase1/verite (10f).

## L'ÉCOLE NE NOTE PLUS — token `20260917a` — décision Rams 17/09 (« go école ») — touche l'apprentissage
**Constat (backup 17/09 00:10 + captures 01:57)** : AA et EV tournent ensemble ; les cycles AA (bougies fabriquées, 10 paires / 2 min, réseau coupé compris) jugeaient les 31 agents — les mêmes qu'en EV : 15/15 dernières lignes Learn = cycles AA, 60/60 jugements de chaque fenêtre en 12 min, le DAO bougeait pendant « Connexion perdue ». La fitness glissante, la compétence par paire, le régime, le génome et le poids des voix en EV étaient notés à ~90 % sur une marche aléatoire.
**Livré** : `learnFromOutcome` (03) sort en première instruction quand `S.tradingMode === 'sim'` (compteur `S._simLearnSkipped`) : plus aucune écriture — fitness, score, confiance, mémoire, `agentPairSkill`, `regimeFitness`, leçons, évolution — depuis l'AA. `_judgeDisciples` (12) ne note pas le jury des disciples en AA. AA reste un bac à sable et une vitrine (ses trades, ses stats, son P&L continuent). HTML → `20260917a` (79). `banc-fitness-glissante.js` 9/9 (S3 statique, D6 dynamique), `banc-phase1-vote-paire.js` : ses tests de jugement passent en `paperReal` (22/22). Pins : gel-backup (03), phase1 (03/12).
**Effet visible** : le DAO ne bouge plus pendant une coupure ; les fitness évoluent au rythme des résolutions EV (≈ 11 par quart d'heure) et des trades — plus lentement, mais sur du vrai. Les 60 jugements AA encore dans les fenêtres sortent au fur et à mesure (≈ 1 h 30 de résolutions EV pour un renouvellement complet).
**Suite prévue (dans cet ordre, ma reco)** : 1) **A13 moteur de sortie** — les SL/TP ATR de 09d1 (bras A/B) ne sont jamais exécutés pour les positions auto : une seule règle de sortie, mesurée (12 h) · 2) **micro-mission P&L** — « portfolio » de l'accueil hors positions, `_startPortfolio = portfolio`, `pnl24h` faux (12 h) · 3) **A14 données réelles pour whale/flow/volume** — carnet Binance `depth`/`bookTicker` au lieu des corps de bougies (24 h) · 4) **génome de paire** — périodes TA (RSI 14, MACD, Bollinger, ADX) et `harmonic_v1` (12 h) · 5) **phase 2 A2/A5** — bus `ps.intel` + attribution par SOURCE de données (quelle donnée rapporte) (24 h) · 6) stabilité : `boots[].doc`, bouton 15 s, et surtout les réglages du tablet (le Guardian conclut « suspension OS »). Le KPI EV se lit à 7 jours de tour propre.

## RETRAIT DE redistributeFitness — token `20260916e` — décision Rams 16/09 (« retire si tu juges inutile »)
**Retiré** : `redistributeFitness` (02, 70 lignes : érosion des saturés / redistribution aux faibles toutes les 40 s + versement du surplus des bots > 1 600 aux disciples), son appel dans le battement (08) et le hook `_payBotSurplus` (12). Motif : depuis la fitness glissante (`20260916c`) aucun siège ne dépasse 1 350 et toute écriture directe de fitness est écrasée au jugement suivant — la fonction n'avait plus d'effet ; la garder était du code mort (règle 4). Le plafond unique 1 600 (règle Rams 03/09) reste vrai par construction ; libellé de la sonde Guardian « Disciples » mis à jour.
**Adapté** : les 3 revigorations de 03 (auto agents, auto bots, manuelle : ≤ 80 T$ remis à 400) vident aussi la fenêtre de jugements — sinon la fitness retombait au jugement suivant et la revigoration ne servait à rien.
**Fichiers** : 02, 08, 12, 03, guardian-core (libellé), HTML → `20260916e` (79). `banc-fitness-glissante.js` S2 (7/7). **Faute de procédure, dite** : le commit `cf174fe` a été poussé alors que banc-all disait BLOQUÉ (S2 en échec : la revigoration des BOTS ne vidait pas la fenêtre, et le banc comptait mal) — l'enchaînement `banc-all && commit` n'était pas gardé sur le verdict. Corrigé dans le commit suivant (revigoration des bots + banc), et la commande de push exige désormais `VERDICT : LIVRABLE` (grep -q). Pins : gel-boot (08), gel-backup (03), phase1 (02/08/12/03), verite (02/08).

## POIDS PAR ATTRIBUTION — token `20260916d` — point 4 du conseil (Rams : « Go 4 ») — touche le consensus
**Règle** : le poids d'un siège dans le consensus d'une paire = **base (fitness glissante : 0,5 + fitness/1000) × compétence sur CETTE paire × compétence dans le RÉGIME courant**, en continu et bayésien léger : f = 0,5 + (succès + 5)/(total + 10) → 1,0 sans historique, 1,2 à 30/10, 0,8 à 10/30, 1,25 à 40/50 ; un petit échantillon tire vers le neutre (1/0 → 1,045). Remplace les paliers ×1,3/×0,6 (corrections/errors, redondants avec la fitness glissante) et ×1,25/×0,75 (paire, ≥ 10 échantillons). Série perdante ÷ 2 et probation ÷ 2 conservées. Régime calculé UNE fois par roster (`_regimeNow`). **`ps.roster.weights[id] = { w, base, pairF, regF }` + `ps.roster.regime`** (RAM) : l'attribution de chaque décision est lisible.
**Vérité corrigée au passage (dans le périmètre : c'est l'entrée du facteur régime)** : `regimeFitness` comptait le résultat du TRADE pour tous les votants (|vote| > 0,05) — d'où les 21 sièges à « calm 282/498 » identiques (audit #19) ; il compte désormais le vote ALIGNÉ du siège (signe du vote × P&L) : wins/total = compétence propre dans ce régime, sumPnl = ce que ses votes auraient rapporté. Les compteurs existants (hérités, copiés) se diluent au fil des trades.
**Fichiers** : 03 (`_attributionFactor`, roster, learnFromOutcome), HTML → `20260916d` (79). `banc-attribution.js` (NOUVEAU, 4/4, bloquant) ; phase1 adapté (22/22). Pins : gel-backup (03), phase1 (03).
**Bilan du conseil 1 → 4 (livré le 16/09 en une journée, à la demande de Rams)** : 1 rien n'est effacé (`20260916a`) · 2 génome réel par siège, 72 gènes (`20260916b`) · 3 fitness glissante sans saturation (`20260916c`) · 4 poids par paire × régime (`20260916d`). Ce que ça promet : adaptation continue mesurée sur du vrai — pas une progression garantie. Ce qui manque encore pour le voir : l'uptime (réseau du tablet : lire `perfLog.net` au prochain backup) et le temps (1 fusion/h, 60 jugements par siège pour une fitness pleine).
**Décision Rams en attente** : retrait de `redistributeFitness` (02:710, économie bots 15/08, sans effet désormais). **Reste du plan** : micro-mission affichage P&L (portfolio hors positions, `_startPortfolio`), phase 2 A2/A5 (bus `ps.intel` + attribution par SOURCE de données — différent de l'attribution par siège livrée ici), A13 moteur de sortie (SL/TP ATR jamais exécutés pour les positions auto), génome de paire (périodes TA), harmonic_v1 génomé.

## FITNESS À FENÊTRE GLISSANTE — token `20260916c` — point 3 du conseil (Rams : « Go 3 ») — touche la sélection
**Règle** : la fitness d'un siège = **350 + 1 000 × E**, E ∈ [−1, +1] = espérance nette de ses **60 derniers jugements** (`a._judgments`, ±1 pondéré par force du signal × amplitude × décroissance ; bots : ±1 × amplitude du trade). Poids **symétriques** : une erreur pèse autant qu'une bonne réponse de même amplitude (avant : +42 / −18 → un pile-ou-face montait). Toujours-juste → 1 350 (le plafond 2 000 n'est plus atteignable) ; pile-ou-face → 350 ; un siège à 80 % qui se met à perdre passe sous 600 en < 30 jugements ; moins de 5 jugements → fitness de naissance conservée ; à la fusion (07) la fenêtre repart de zéro (elle mesure le génome courant). Plus de saturation, pression permanente, oubli des vieux régimes. `totalReward`, `errors`, `corrections`, `streak`, `conf`, `score` : inchangés. Bonus de série sur la fitness retiré (il était écrasé au jugement suivant ; la série pèse toujours dans le roster).
**Conséquence déclarée** : `redistributeFitness` (02:710, économie des bots 15/08 : surplus > 1 600 versé aux disciples ; érosion/redistribution des hybrides) ne déclenchera plus jamais son volet « surplus » (fitness ≤ 1 350 par construction) et son volet « érosion » est écrasé au jugement suivant → à retirer sur décision Rams (c'était son architecture). Les fitness actuelles (bots ~500, hybrides 1 600) glissent vers leur vraie valeur au fil des 5 premiers jugements après la relance.
**Fichiers** : 03 (`_fitJudge`, 4 écritures remplacées, bonus retiré), 07 (fenêtre à zéro à la fusion), 09b1/09b2 (`_judgments`, `_probationUntil`, `_bornCycle` persistés), HTML → `20260916c` (79). `banc-fitness-glissante.js` (NOUVEAU, 6/6, bloquant), `banc-phase1-vote-paire.js` adapté (22/22). Pins : gel-backup (03), gel-boot (09b1/09b2), phase1 (03), verite (09b2).
**Point 4 suivant** (poids par attribution) : spec ci-dessous.

## GÉNOME RÉEL PAR SIÈGE — token `20260916b` — point 2 du conseil (Rams : « Go 2 ») — touche la DÉCISION dès la première fusion
**Ce qui existe maintenant** : 17 sièges (9 scouts, 7 conseillers, le gardien sécurité) portent un génome = les nombres que leur logique lit réellement : fenêtres, seuils RSI/ADX/cv/Bollinger, gains, poids propre/conseil, seuil de vote (72 gènes au total). `GENOME_DEFAULTS` (03) = les constantes qui étaient en dur → **comportement byte-identique par défaut, prouvé** : `banc-genome.js` rejoue les trois fonctions livrées contre l'ORACLE `banc-fixtures/analyse-avant-genome-20260916a.js` (texte exact d'avant génome) sur 40 états aléatoires × 3 paires × 21 sorties : identiques au caractère près. `S.genome[id]` = version vivante, `S.genomeHistory[id]` = ≤ 10 meilleures versions passées avec leur fitness de pointe (sauvegardés, manifest).
**Évolution réelle** (07 `triggerEvolution` → 03 `_genomeEvolve`) : à chaque fusion (1/h), la version courante du siège est archivée avec sa fitness de pointe, puis le nouveau génome naît gène à gène par recombinaison entre la version courante et la MEILLEURE version passée DU MÊME SIÈGE, puis mutation ±`_mutation` (4-16 %, déjà calculé sur la diversité), bornes par gène (fenêtres entières 2-60, seuils dans leur domaine, sinon [défaut/4, défaut×4]). Rien n'est copié d'un siège à l'autre. Journal 🧬 « Génome ID : n/N gènes mutés (±p %) · version précédente archivée (pointe X T$) ». **Probation** : `_probationUntil = _bornCycle + 30`, poids ×0,5 dans le roster (03) pendant 30 résolutions.
**Pas génomés** (dit clairement) : macro/fundamental (neutralisés S3), nlp_v1 (news, pas de constante), harmonic_v1 (06 `detectHarmonicResonance`, à génomer plus tard), les bots et l'Évolueur ; les périodes de `getTechSignals` (RSI 14, MACD, Bollinger…) restent globales (une TA par paire, partagée) — génomable plus tard comme « génome de paire ».
**Fichiers** : 03 (registre + moteur + lectures `G.` dans scoutAnalysis/councilVote/guardianCheck + probation), 07 (hook), 09b1/09b2 (persistance + manifest), HTML → `20260916b` (79), `banc-genome.js` (NOUVEAU, 5/5, bloquant), `banc-fixtures/analyse-avant-genome-20260916a.js` (oracle, ne pas modifier). Pins : gel-backup (03), gel-boot (09b1/09b2), phase1 (03), verite (09b2).
**Ce que Rams verra** : rien avant la première fusion (≤ 1 h après la relance) ; puis une ligne 🧬 par fusion ; les votes d'un siège muté changent (banc 4 le prouve). Le premier génome archivé de chaque siège = la version d'avant (fitness de pointe historique) : la recombinaison peut donc toujours y revenir.
**Point 3 suivant** (fitness glissante, spec ci-dessous) puis 4.

## 1c-FULL « LE SAVOIR RESTE AVEC LE SIÈGE » — token `20260916a` — point 1 du conseil « évolution à l'infini » (Rams 16/09 : « je ne veux pas attendre le 22, je veux appliquer 1 à 4 »)
**Règle nouvelle (remplace celle du 06/09)** : la logique de vote est par siège (id) ; la fusion ne change que nom, score, confiance. Donc à la fusion **rien n'est effacé ni copié** : `memory`, `agentPairSkill[id]`, `discipleTaskSkill[id]`, `regimeFitness` restent au siège. Fitness de naissance = max(350, moyenne des parents du tournoi / 2). `errors`/`corrections` conservés (bilan de la logique), `streak` remis à zéro, `fitnessHistory` continué. La succession auprès des bots (12 `_onAgentEvolved(id, nom)`) ne déplace plus que l'affectation du siège : l'héritier garde son savoir, le nouveau-né (retour en pépinière) garde le sien. Explosion de masse du 06/09 impossible par construction (rien n'est dupliqué) ; plafond 500 par cellule (03, 12) inchangé.
**Fichiers** : 07 (`triggerEvolution`), 12 (`_onAgentEvolved`), HTML → `20260916a` (79). `banc-skill-borne.js` réécrit pour la règle du 16/09 — **et réparé** : son pin HTML figé sur `20260906i` le faisait s'arrêter à sa 1re section depuis le 06/09 (sections 2-4 jamais rejouées, toléré dans CONNUS) ; il lit DOC_V, **33/33**, retiré de CONNUS. `banc-verite-donnees.js` **24/24** (D4 rejoue banc-skill-borne, D5 statique : plus aucun reset ni transfert). Pins : phase1 (12).
**À voir** : ligne 🎓 « Héritage : X reprend le siège de feu Y » sans plus de « cellules » ; onglet Agents : la compétence par paire d'un siège ne retombe plus à 0/0 après une fusion.

## Conseil « évolution à l'infini » — points 2, 3, 4 (spécifiés, à livrer dans cet ordre, sans attendre)
**2 · Génome réel par siège** (touche la décision, 24 h) : chaque siège reçoit `S.genome[id]` = les nombres que sa logique lit réellement dans `scoutAnalysis` (03) et `getTechSignals` (08) — périodes et seuils RSI, longueurs de moyennes, seuil ADX, poids dans le composite, multiplicateurs par régime, facteurs TP/SL de `_resolvePairCycleCore`. Étape a) inventaire : extraire les constantes en dur de chaque `case` de `scoutAnalysis` et de `getTechSignals` dans un génome par défaut (comportement byte-identique, banc de non-régression = mêmes votes qu'avant sur un snapshot) ; b) l'Évolueur mute (`_mutation` réel, ±% par gène) et recombine les gènes **du même siège** (ses meilleures versions passées, conservées dans `S.genomeHistory[id]`), fitness choisit ; c) probation : un nouveau-né vote à poids réduit pendant `_bornCycle` + 30 résolutions.
**3 · Fitness à fenêtre glissante** (touche la sélection, 12 h) : `a.fitness` reste l'échelle affichée (50-2 000) mais devient 350 + 1 000 × espérance nette normalisée des N=60 derniers jugements réels du siège (par régime), au lieu de l'addition +42/−18 plafonnée : plus de saturation à 1 600, pression permanente, oubli des vieux régimes. Bancs : plafond jamais atteint sur 1 000 événements, un siège qui se met à perdre redescend en < 30 jugements.
**4 · Les poids suivent** (= phase 2 A5, 24 h) : poids d'un siège dans le consensus d'une paire = sa fitness récente sur cette paire dans ce régime (`agentPairSkill` × `regimeFitness`), via le bus `ps.intel` et `S.attribution[src][mode]`.
Promesse tenue par ces trois lots : adaptation continue mesurée sur du vrai — pas une progression garantie.

## BUNKER SUR L'EQUITY — token `20260915d` — dernière retouche avant les 7 jours (« go » Rams 15/09 23:10)
**Constat (capture Rams 15/09 23:00)** : « 🚨 BUNKER : capital −15,2 % (90,06 $) · reduceMises, sendTelegram, saveBackup » deux minutes après la relance, avec 3 positions ouvertes et une equity intacte (~117 $). Cause au code : `checkBunker` (07) mesurait `S.tradingAccount` seul ; la mise d'une position sort du compte à l'ouverture et n'y revient qu'à la clôture → 3 positions = −27 $ « perdus » → alerte, et **reduceMises appliqué** (mises divisées) sur une perte inexistante — à chaque fois que 3 positions sont ouvertes. Même défaut que le « portfolio » de l'accueil (AUDIT #23).
**Livré** : `js/07-v90-mode-bunker-sos.js` — `_bkCapital()` = compte trading + valeur courante des positions ouvertes du mode (repli : la mise) ; référence, seuil de déclenchement, récupération (`_bkAutoCheck`), bannière, `exitBunker` et les deux messages lisent cette valeur (8 lectures du compte seul remplacées, 0 restante dans le bunker). Seuils inchangés (`triggerDropPct`, `recoveryPct`, `minStakePct`). HTML → `20260915d` (79). `banc-verite-donnees.js` **22/22** (D13 `checkBunker` RÉEL : 3 positions ouvertes → rien ; vraie perte de 16,1 % → bunker). 08/03/10g/10f/02/09b2/13 byte-identiques.
**Effet à la relance** : `_bkInitCapRef` repose la référence sur l'equity courante ; le bunker actif depuis 23:00 se lève quand `_bkAutoCheck` voit la récupération (ou manuellement, bouton « lever ») et les mises reviennent à leur valeur (`_bunkerOrigStake`). Si la bannière reste, Rams la lève à la main : le prochain déclenchement ne pourra venir que d'une vraie chute.

## PHASE 1b-b « L'ANALYSE LIT LES VRAIES BOUGIES » — token `20260915c` — touche la DÉCISION → observation 24 h puis 7 jours sans retouche
**Ce qui change** : en EV et RE, `ps.candles` (ce que lisent `getTechSignals` = 14 indicateurs / 60 % du composite, les 13 scouts, les patterns 06, le régime 02:661) = **les 60 dernières klines Binance de la tf du mode (15 m)** — la même série que la porte 10g/08 — projetées à chaque passage du mode dans le battement (`_projectRealCandles`, 08 ; mode affiché à chaque tick, arrière-plan 1 tick sur 3, avant `_botExitSweep`). Chaque bougie porte son `ts`. Le générateur de marche aléatoire (« New candle », 08) est **réservé au mode AA** : il ne fabrique plus de bougies pour EV/RE et n'écrase plus `ps.price` avec un close synthétique toutes les 3 s. Série périmée (critère `_realCandlesStale`, le même que les portes) → `ps.candles` figé + `ps._candlesStale = true`, la porte refuse déjà d'ouvrir. `ps.pnl24h` EV/RE = variation réelle de la fenêtre (15 h), borné ±40 %. Écart à la spec (5 m) : la tf du mode, parce que c'est elle que la porte lit et la seule bootstrappée/refetch (1b-a) — une série, une vérité.
**Heatmap** (`03 recordTradeForHeatmap`) : clôtures EV/RE seulement, AA exclu, remise à zéro unique du compteur mélangé (~2 000 trades AA pour 47 EV → la porte P3 lisait du bruit). P3 reste neutre tant qu'un créneau n'a pas 20 vrais trades : c'est l'état honnête.
**Fichiers** : 08 (projection + garde du générateur), 03 (heatmap), 10e3 (limite déclarée mise à jour, cartouche), HTML → `20260915c` (79). `banc-verite-donnees.js` **21/21** (D7 `_projectRealCandles` RÉEL : projection, ts, figée si périmée, AA intact, pas de réallocation, pnl24h ; D8 `recordTradeForHeatmap` RÉEL + générateur réservé à sim). Pins : gel-boot (08), gel-backup (03), phase1 (03/08). 2 tests ⏳ restants = 1c-full (décision Rams).
**Ce que Rams verra** : les sparklines des briques EV = vraies bougies 15 m ; RSI/MACD/ADX cohérents avec le marché ; PEPE analysé à son vrai prix (3,4e-6, plus 2,6e-7). Les indicateurs changent d'horizon (RSI 14 sur 15 m = 3 h 30 au lieu de 42 s de bougies inventées) : **les votes et le nombre d'ouvertures vont changer — c'est le but**. Le KPI (espérance nette EV) se lit à 7 jours, pas avant.
**Observation** : backup après 24 h (11 paires vivantes, `ps.candles[*].ts` présents en EV, `heatmap._realOnlySince` posé, cycles qui résolvent) ; puis **7 jours sans retouche** et le chiffre. Reste ouvert : 1c-full (héritage par ID, décision Rams), micro-mission P&L, phase 2, stabilité réseau (lire `perfLog.net`).

## 1c-LITE + VEILLE CSS — token `20260915b` — même soir (aucune décision d'ouverture/fermeture touchée)
Livré sans attendre 24 h, à la demande de Rams (« tu ne répares rien »). Quatre racines de l'audit, toutes hors décision :
- `js/07-v90-mode-bunker-sos.js` — **évolution 1 fusion / h** (était 1 / min : 95 013 générations, chaque siège recyclé toutes les ~5 h) ; **type/source du siège restaurés** via `_SEAT_DEF` (21 sièges, littéral initial de 02) au lieu de la concaténation des parents qui donnait « Fade·Fade / RSI·Sentiment » partout — la logique de vote est par id (03), elle n'a jamais changé ; **`regimeFitness` du siège conservée** (plus la copie 60/40 des parents : les 21 affichaient « calm 282/498 » identique) ; **rêve 1 / jour** (était 4 min : evoLog 50/50 = rêves, `ps.threshold` ratcheté à 0,82). Fitness 350 à la naissance et reset mémoire/skill du siège INCHANGÉS : le banc `banc-skill-borne.js` (règle Rams 06/09) les verrouille, et le vrai héritage (la compétence reste avec l'ID, la succession ne déplace que le siège) contredit ce banc → décision Rams requise avant 1c-full (voir Reports).
- `js/09b2-save-load.js` — `_auraRotatePurge` (toutes les 2 min) : les 6 historiques rotatifs gardent le plus RÉCENT (`cutEnd`) au lieu du plus vieux (`cut`) : `learningHistory` (figé au cycle 130 980 depuis juin), `globalMemoryPool`, `fiscalReserveLog`, `dreamJournal`, `w.dreamJournal`, `w.antiNegReserveLog`. L'onglet Learn va enfin bouger.
- `js/13-veille-ecran.js` — réécrite en CSS : plus de canvas 60 fps (resize/frame, 260 traînées, shadowBlur ×31, eval/frame ; sonde LENT 1,0-1,8 s par frame les 12, 14 et 15/09) ; 60 étoiles en divs animées par le compositeur, AURA, heure + P&L toutes les 30 s ; mêmes déclencheurs, même sortie, même wake lock. Design 16/08 respecté dans l'esprit ; si Rams veut ses étoiles filantes, ce sera en CSS aussi.
- HTML → `20260915b` (79). `banc-verite-donnees.js` **19/19** (S5, S6, D6 `_auraRotatePurge` RÉEL, S11 activés ; 4 encore en attente : D4/D5 = 1c-full, D7/D8 = 1b-b). Pins : gel-boot (09b2), verite (09b2). 01/02/03/08/09b1/10f/10g/12 byte-identiques.
**À voir dès la relance** : noms des agents inchangés mais types réels dans l'onglet Agents (Macro-Économie → Linear·FRED, etc.) au fil des prochaines fusions (1/h) ; Learn : historique qui avance ; journal : plus de 💤 toutes les 4 min ; veille : fond étoilé CSS après 10 min, plus aucune ligne LENT `rAF@13-veille-ecran.js`.
**Reste pour que le système décide sur du vrai : 1b-b** (analyse EV/RE sur klines — touche la décision, spec inchangée ci-dessous). Puis 1c-full (héritage par ID) sur décision Rams, P&L, phase 2.

## SONDE RÉSEAU — token `20260915a` — 12 h (aucune décision touchée) — le plan se plie au backup du 15/09
**Vérification 1b-a (backup `aura_guardian_full_20260915-180411.json`, cycle 605 942)** : **11/11 paires EV avec bougie 15m de 4 min** (hier matin 3/12), 5m vivantes partout, GBP désactivée, 0 position zombie (DOT/DOGE/EUR fermées), 17 trades EV en 24 h sur SOL/DOT/EUR/LINK/ETH (avant : BTC/ADA/EUR seules), equity EV 117,61 $ (hier 118,6 — plat), `paperRealStats` non gonflées (DOT 5 trades), aucune ligne « ⚠ Fermeture non aboutie » dans les 50 lignes conservées (la racine de la chute des bots reste sans nom : le journal ne couvre que 10:23 → 18:03). Bots remontés à ~500 T$. **1b-a : livrée et vérifiée.**
**Ce que le backup montre d'autre — la stabilité bloque toute observation** : cycles à 60-70/10 min de 21:00 à 05:18, puis **05:28 → 09:24 figé** (trous 104 + 139 min, op `fetch ping`/`saveState`, `jsN 40`, WS 0-3 msg) et **10:48 → 18:03 figé** (⏸ 10:44:27 « Évaluation en pause », plus aucune ligne 📡 CoinGecko après 10:42, heap toutes les 10 min jusqu'à 13:38, dernière sauvegarde 13:47, relance manuelle 18:03). Soit ~11 h sur 21 sans trading, deux jours de suite. 🐌 10:31:49 « JS inactif (suspension OS) » (82 s, `jsN 0`). Veille écran toujours à 1,0 s/frame (12:35, 13:15). La 📡 CoinGecko qui s'arrête en même temps que Binance dit « réseau du tablet coupé » plutôt que « Binance bloque » — mais rien ne le PROUVE : `_netPing` ne journalisait rien, et pire, **un statut HTTP 418/429 (IP bloquée) comptait comme « en ligne »**.
**Livré** : `js/01-chrono-network.js` — chaque ping classé (`ok` / `http STATUT` / `abort` 5 s / `error (message)`) dans `S.perfLog.net` (60 derniers, sauvegardés) ; HTTP ≥ 400 = échec ; au 2e échec, témoin CoinGecko puis journal `📵 Réseau · Binance http 418 ×2 · CoinGecko ok 200 → Binance seul injoignable · trading en pause` (ou `… → réseau du tablet coupé`) ; au retour `📶 Réseau rétabli après N min · Binance ok en X ms` ; `window._auraNetOffline`. `js/02-state-init.js` — gardien des WS : rien hors ligne, remplacement d'un WS fermé/fantôme seulement après le backoff partagé `_bgNextTry` (le gardien 5 s + le retry onclose faisaient ~130 connexions/min sur 11 paires pendant une coupure ; limite Binance 300 / 5 min / IP). `09b1`/`09b2` : `perfLog.net` sauvegardé/relu. HTML → `20260915a` (79). `banc-verite-donnees.js` **15/15** : S10 statique + D12 `_netPing` RÉEL (fetch simulé : 418 classé, témoin, 📵 une fois, 📶 avec durée, « réseau du tablet coupé »). Pins : gel-boot (09b1/09b2), phase1 (02 ; 10f pinné `20260914b`), verite (02/09b2 ; 10f `20260914b`). 10f/08/10g/03/07/12 byte-identiques.
**Observation (dès la prochaine coupure)** : `perfLog.net` + lignes 📵/📶 → le backup NOMME la cause (statut Binance, CoinGecko, durée). **Rams, côté tablet, à faire avant la nuit : Paramètres → Batterie → l'app AURA (et Chrome/WebView) en « Sans restriction » ; Wi-Fi → « Maintenir le Wi-Fi activé en veille : Toujours » ; désactiver « Mettre en veille les applications inutilisées ».** Sans ça, la sonde dira « réseau du tablet coupé » et il n'y aura rien à corriger dans le code.
**Prochaine mission** : 1) lecture du backup (perfLog.net) après 24 h → cause nommée ; 2) **1b-b** (spec ci-dessous) ; 3) 1c ; 4) P&L ; 5) phase 2. Si la sonde dit « Binance seul » : lot stabilité (veille écran en CSS, resume plus tolérant) avant 1b-b.

## HOTFIX 1b-a — token `20260914b` — 14/09 21:58 : les 9 bots au plancher 50 T$ (capture Rams, page DAO)
**Constat** : 6 h après la relance sur `20260914a`, les 9 bots affichent 50 T$ (plancher `Math.max(50, …)`, 03:1246) ; ils étaient entre 420 et 1 567 le matin ; l'Évolueur et plusieurs hybrides à 400. Cette chute exige ~170 jugements perdants (−8,6 T$ par bot par perte en EV) : impossible avec 3 positions.
**Cause (prouvée au code, racine exacte à nommer au journal)** : `closePosition` (02:5657) sauvegarde l'état (`_p5PreActionSave` = sauvegarde complète), incrémente les stats, appelle `learnFromOutcome('position')` et ne RETIRE la position qu'en dernier (`S.openPositions = …filter`, ~200 lignes plus bas). Si une ligne lève entre-temps, la position reste ouverte. Le résolveur réessayait au mieux toutes les 60-120 s ; `_botExitSweep` (1b-a) réessayait **à chaque tick** : sauvegarde + stats + pénalité pour tous les agents, une fois par seconde, exception avalée par le `catch`. Ma faute de conception : j'ai porté une vérification prix à la cadence du tick sans vérifier que la fermeture aboutit. Ce qui lève dans `closePosition` pour cette position (DOT, pyramidée à la main, contexte du 12/09) n'est PAS connu : le journal ne le disait pas — il le dit désormais.
**Livré** : `js/10f-resolveur-cycle.js` — `_closeCompleted(pos, label)` (source unique pour `_botExitSweep` et `_lossCapSweep`) : appelle `closePosition`, VÉRIFIE que la position a disparu ; sinon `pos._exitFailAt` (1 nouvel essai / 60 s), aucune leçon, `_decErr`, et UNE ligne journal (puis 1 sur 10) : « ⚠ Fermeture non aboutie · PAIR · bot SL −0,9% · essai N · TypeError: … @ <1re ligne de pile> » ou « closePosition sans erreur mais position toujours ouverte ». `banc-verite-donnees.js` 13/13 (D11 : lève / ne retire pas → 1 appel malgré 5 ticks, 0 learn, journal avec la raison ; réussie → learn). HTML → `20260914b` (79). Pins : net-expectancy (10f `20260914b`, 693 lignes), phase1 et verite (02/08/10g/09b2 restent `20260914a`).
**Dégât** : la fitness des bots (et des hybrides touchés) n'est pas restaurable — on répare en avançant (règle 9) ; les bots regagnent sur les trades gagnants (+mag×5). `paperRealStats[DOT].trades/pnlNet`, `abTesting` et `S.paperRealConsecLosses` ont probablement été incrémentés à chaque essai (à lire dans le backup : DOT trades ≫ 2, pnlNet ≪ 0) → à remettre d'aplomb dans 1c avec le patrimoine.
**Observation (immédiate)** : journal après relance → la ligne « ⚠ Fermeture non aboutie · DOT/USDT … » avec sa raison = LA donnée à envoyer (avec le backup). Si Rams ferme DOT à la main et voit un ❌, c'est la même racine.

## Phase 1b-a du matin — token `20260914a` — 24 h d'observation (inchangée sauf le hotfix ci-dessus)
**Le problème (audit 14/09, prouvé au code et au backup)** : 7 paires EV sur 12 sans bougie 15m depuis le 13/09 21:45 (17 h) à cause de deux verrous — le filtre outlier de l'agrégation WS comparait chaque prix au dernier close **5m**, série jamais re-bootstrappée (ETH 5m à 1 919 du 19/08 pour un prix à 2 501 → 100 % des prix rejetés, toutes tf, à vie) ; et le refetch « données obsolètes » des portes EV/RE était placé APRÈS `if (closedTs <= lastSeenTs) return`, donc inatteignable sur une série figée. Conséquence : porte jamais évaluée et **sorties bot jamais vérifiées** (TP/SL/breakeven vivaient dans `_resolvePairCycleCore`, exécutée seulement quand une bougie Binance se ferme) → DOT et DOGE sous leur SL depuis le 12/09, 3 slots/3, zéro ouverture. `_lossCapSweep` n'avait plus d'appelant depuis le 26/07. GBP/USDT tournait sur des klines de décembre 2023 (paire retirée de Binance le 29/12/2023, avis officiel).

### Fichiers livrés (js/ + racine ; HTML dans le lot)
- `js/02-state-init.js` : `_rcLastPx` + `_rcOutlier(pair, price, ts)` = source unique du filtre outlier (@trade, @kline, modal) : référence = dernier prix RÉEL accepté de la paire s'il a < 5 min, > 2 % → rejet ; sans référence fraîche le prix est accepté et devient la référence (le filtre se répare seul en 5 min au lieu de se verrouiller). Le bloc `ref5m` de `_aggregateRealPrice`, la référence par intervalle de `_aggregateRealPriceOtherIntervals` et la référence `close || ps.price` de `_upsertKlineCandle` sont retirés (la cohérence h/l vs close des klines reste). `_realCandlesStale(pair, tf)` (série absente, < 30 ou bougie en cours > max(2,5 tf, 2 min) = critère des portes) ; `_startBgCollector` bootstrappe TOUTE paire active dont la série est périmée (plus seulement « nouvelle ou pausée ») ; `_fetchAndBootstrapRealCandles` : limiteur 1 appel / 90 s / paire·tf tous appelants confondus, le dernier close REST devient la référence du filtre, op de sonde renommé `bootstrap:PAIRE` (sans espace).
- `js/10g-resolveur-ev-csv.js` (porte EV) et `js/08-learning-history-render.js` (porte RE) : test de fraîcheur (`arr.length < 30 || dataAge > stalenessThreshold` → refetch + return) placé AVANT `closedTs <= lastSeenTs`. Seuils inchangés.
- `js/10f-resolveur-cycle.js` : nouveau `_botExitSweep()` = TP / SL / breakeven des positions auto sur `ps.price`, sémantique INCHANGÉE (SL immédiat, TP après 5 cycles, breakeven-stop à 45 % du TP, `canBotClose = S.botAutoMode !== false`, `learnFromOutcome('trade')`, toast, LMSR réinitialisé). `_resolvePairCycleCore` calcule toujours `tpPct`/`slPct` (conviction + volatilité du moment) et les ÉCRIT sur la position (`_tpPct`, `_slPct`) ; il ne garde que « Signal inversé » / timeout / hardTime (0 `tpHit`/`slHit` dans le résolveur). Position antérieure sans cibles : repli sur la conviction d'ouverture.
- `js/08` (battement) : `_botExitSweep()` appelé pour le mode traité juste après `_applyPaperRealProtection` (mode affiché à chaque tick, arrière-plan 1 tick sur 3) ; `_lossCapSweep()` rebranché 1 tick sur 3 (plafond inchangé : 2 × SL prévu, borné 1,5-3 %, manuelles comprises). **Tranché : la règle du 06/07 (« stoppe si perte trop importante, même si je l'ai oublié ») prime sur celle du 27/07** — Rams a dit « go 1b-a » sans la maintenir.
- `js/09b2-save-load.js` : `_EV_DELISTED_PAIRS = ['GBP/USDT']`, `_evRetireDelisted()` à chaque chargement (idempotent) : activation EV/RE retirée + WS fermé ; GBP reste en AA. Ligne journal `09b2·delisted` au 1er chargement.
- `AURA8_v118.html` : `DOC_V` + 78 `?v=` → `20260914a` (79). Diff = token seul.
- `banc-verite-donnees.js` (NOUVEAU, **12/12, bloquant dans banc-all**) : S1-S4, S7-S9 statiques + D1 `_aggregateRealPrice` RÉEL (5m figée à −30 % → prix accepté, glitch +4 % rejeté, auto-réparation), D2 `_resolvePaperRealCycle` RÉEL (série figée déjà vue → refetch au tick suivant ; fraîche → porte atteinte), D3 `_botExitSweep` RÉEL (7 cas), D9 `_evRetireDelisted`, D10 `_realCandlesStale`. 7 tests affichés ⏳ non comptés, activés par leur phase : S5/S6/D4/D5/D6 (1c), D7/D8 (1b-b).
- Pins adaptés : `banc-gel-boot.js` (08, 09b2), `banc-net-expectancy.js` (10f `20260914a`, 659 lignes ; reste 18/19 CONNU), `banc-phase1-vote-paire.js` (02/08 en-tête 1b-a, 03/12 inchangés). `banc-all` : LIVRABLE, 0 échec nouveau.
- Non touchés (byte-identiques) : 03, 07, 09c, 09d1, 09b1, 10d, 10e*, 12, css, guardian-*.

### Ce que ça CHANGE (déclaré, rien de caché)
- En AA aussi, TP/SL des positions bot sont désormais vérifiés à chaque tick (avant : à la résolution du cycle, ≤ 120 s plus tard) : sorties au même niveau, plus tôt. Le P&L des positions ouvertes (`pos.pnl`, `pnlUsdt`, `currentVal`) est rafraîchi à chaque tick (utile à la micro-mission P&L).
- Le journal peut montrer des lignes ⛔ « Perte max » (garde-fou rebranché) — attendu sur DOT si Rams ne l'a pas fermé (−2,4 % sur l'entrée > plafond 1,5 %).
- Après la relance : jusqu'à 11 appels REST `klines` (paires périmées, 90 s de limiteur) — lignes 🐌 possibles nommant `op bootstrap:PAIRE` : à envoyer telles quelles si > 1 s.
- `pos.sl` / `pos.tp` (niveaux ATR de 09d1, bras A/B) restent NON exécutés pour les positions auto (ils ne l'ont jamais été) : la sortie réelle est `_slPct`/`_tpPct` (conviction). Le sens de l'A/B sur `slAtrMult`/`tpAtrMult` est donc nul aujourd'hui → moteur de sortie A13 (report).

### Observation attendue (24 h — porte/sorties touchées, loi 5)
1. Journal : `Document v20260914a chargé`, `09b2·delisted — 1 activation(s) EV/RE retirée(s) : GBP/USDT`.
2. **Backup après 24 h : les 11 paires EV actives ont une bougie 15m de moins de 37 min** (`realCandles.*.15m[-1].ts`) — c'est LE critère de réussite ; 12/12 hier matin il y en avait 3.
3. Aucune position auto avec `pos.pnl <= -pos._slPct` depuis plus de 3 ticks ; DOT/DOGE fermées (par Rams ou par le balayage).
4. Cycles EV qui résolvent sur les 11 paires (lignes EVAL/VETO du `brainLog` sur d'autres paires que BTC/ADA/EUR).
5. Guardian : aucune 🐌 nommant `roster:PAIRE` ou `bootstrap:PAIRE` > 1 s ; sinon l'envoyer telle quelle.
6. **Seule donnée à envoyer** : le backup Guardian complet + toute ligne bloqueur. Le KPI (espérance nette EV) se lit à 7 j.

## Audit vérité 14/09 — `AUDIT-VERITE-AURA8.md` (référence de toutes les missions ; 24 entrées, chronologie prouvée de la nuit, procédure §6)
Rappel des racines restantes après 1b-a : analyse EV/RE sur `ps.candles` synthétiques (13/13 scouts, 14 indicateurs, régime, corrélation de repli — PEPE analysé à 1/13 de son prix) → **1b-b** ; agents = clones Fade·Fade (60 fusions/h, mémoire/skill effacées, `regimeFitness` cloné), `09b2:905` `cut` garde le plus vieux (purge toutes les 2 min → `learningHistory` figé en juin), rêve toutes les 4 min qui inonde `evoLog` → **1c** ; veille écran à 1,8 s/frame (`13-veille-ecran.js`) et garde réseau qui met tout en pause sur 2 pings ratés (3 pauses la nuit du 13, dont 62 min) → à trancher après 1b-b (stabilité, 12 h).

## Prochaine mission — dans l'ordre : 1) **phase 1b-b** analyse EV/RE sur klines (24 h) · 2) phase 1c patrimoine des agents (12 h) · 3) micro-mission affichage P&L (12 h) · 4) phase 2 bus/attribution
**1b-b** : en EV/RE, `ps.candles` = klines Binance (5m via `S.realCandles[pair]['5m']`, alimentée par l'agrégation WS désormais vivante + bootstrap au boot étendu à la 5m), le générateur synthétique de `simTick` (08:3164-3207) réservé à AA ; hors ligne → série figée + graphe marqué « hors ligne » (la porte refuse déjà). Fichiers : 08 (générateur + `getTechSignals` inchangé, il lit `ps.candles`), 02 (bootstrap 5m + `blendRealPrices`), 09f3/04 (graphe), banc-verite-donnees D7/D8 activés. Touche la décision → 24 h. Observation : `getTechSignals('ETH/USDT')` en EV sur des bougies avec `ts` ; PEPE : indicateurs calculés sur 3,43e-6 et non 2,6e-7.
**1c** : évolution 1/h max, héritage (fitness moyenne des parents, mémoire + `agentPairSkill` du défunt, `regimeFitness` vide), rêve 1/jour au rollover hors `evoLog`, `09b2:905` `cut` → `cutEnd` (purge 2 min), 📡/💤 hors `chainLog` ; bancs S5/S6/D4/D5/D6.
Phase 2 inchangée = A2 bus `ps.intel` (`10i-intel-bus.js`) + A5 attribution `S.attribution[src][mode]` — lecture seule (12 h). Fichiers : 10i (nouveau), 10f, 09c, 02 clôture, 09b1/09b2 (+ manifest).

## Reports (non traités)
- 1b-a : `pos.sl`/`pos.tp` ATR (09d1, bras A/B `slAtrMult`/`tpAtrMult`) jamais comparés au prix pour une position auto → l'A/B ne mesure rien sur les sorties (A13) ; pyramidage manuel (bouton `pyramidPosition`, 05:534) sur une position auto change `_avgEntryPrice` mais les % TP/SL restent sur `entryPrice` ; `_auraRotatePurge` (09b2:905) tourne toutes les 2 min avec `cut` = tête (1c) ; garde réseau : 2 pings Binance ratés en 40 s → pause de TOUT le trading tant que le ping échoue (01:120-140) — journaliser statut HTTP + durée avant de trancher.
- Audit 14/09 (hors 1b-a/1b-b/1c) : bouton inline `AURA8_v118.html` à 14,9 s le 13/09 21:36 (`BUTTON.onclick`, nom absent de la LoAF → sonde) ; ping réseau sans statut HTTP journalisé → ajouter statut + durée ; `boots[].doc` null ; `chainLog` inondé par 📡 CoinGecko + 💤 rêves (la preuve s'efface en ~1 h) ; `globalMemoryPool` « ADA won:false pnl 0.5 » pour un gain ; `pnl24h` EV 30,2 faux, `_totalCompounded` fossile, `cashLog` EV mort depuis le 07/07, `_startPortfolio = portfolio` (`09b2:610`) ; Sharpe ×1,5 sur 6 trades ; A/B bras B −4,48 $/20 trades ; heatmap tous modes (`10e3`) ; heap 21 → 87 Mo / DOM 5 000 → 9 400 en 24 h.
- Écran blanc / heap 468 Mo : au prochain trou inexpliqué, backup → `perfLog.boots[].prevSavedAt` + `perfLog.heap` ; lire ce qui tourne à +20/+30 min (timers longs, backup FULL +60 s, Dream cycle, `memRecordSession`).
- Churn non décisionnel : `probePersistence` → `buildSnapshot()` 1,5 Mo / 2 min ; `probeStorageSync` relit l'IDB / 2 min ; `09b2` l.144 JSON.parse complet du LS toutes les 25 s pour lire `.cycle`.
- `window.X` posé dans 2 fichiers (banc-all ⚠️ inchangés) : `stopSim` {09i, 01}, `applyTheme` {05, 07}, `_bgResolve` {02, 08}, `_auraLastOp` {00, 08}, `requestAnimationFrame` {00, 03}, `GUARDIAN_CONFIG` {guardian-config, guardian-core}.
- `banc-skill-borne.js` (token 20260906i en dur) et `banc-p7-news.js` (payload absent) figés → à réécrire « token lu dans le HTML » ; `net-expectancy` 18/19 et `p6` 28/29 tolérés par `CONNUS`.
- `_APPLYSNAP_MANIFEST` liste `_errStats`, `_riskVetoes`, `_botSurplusCarry`, `_fpByBot` que 09b1 ne sauvegarde pas ; `sw.js` jamais enregistré ; `alert('⛔ Export REFUSÉ…')` → modal ; `probeFiles` réseau ne teste pas les CSS `?v=` ; `boots[].doc` toujours `null` (exposer `window.DOC_V`) ; `S.cycle` par paire ; Évolueur 3 générations en 3 min ; leçons ×0.008 (A8) ; `executePending` plancher 10 $ (A4) ; `10-fin-bloc` mort (A11).
- Hors code — Rams : vieux backups `Download/AURA` (28/06), DriveSync sans dépôt Drive depuis août, révoquer ancienne clé CoinStats + ancien PAT classique, GitHub 2FA, tablette branchée en permanence + Samsung : retirer AURA de la mise en veille des applis et de l'optimisation batterie.

## Rappels techniques
- Position LoAF = unités UTF-16 (`src.encode('utf-16-le')[:pos*2]`), pas `src[:pos]`. `grep` mono-ligne rate les timers multi-lignes.
- IDB : jamais `getAll()` / `openCursor()` sur un store de payloads. `performance.memory` quantifié : tendances, pas des Mo exacts.
- `S` est un `const` de script (`02-state-init.js`) : pas de `window.S` ; modes `sim`/`paperReal`/`real` et clés de stockage (`nexus_state_v2`, `nexusSnap_A/B/C`, `NEXUS_DB`) jamais renommés. `pairStates` est multiplexé par mode (`_WALLET_ACCESSOR_FIELDS`) : `ps.roster` est donc par paire ET par mode.
- Remplacements par ancre UNIQUE assertée (`s.count(old)==1`), jamais `String.replace` ; en-têtes de fichier pinés par les bancs (03 : gel-backup, 08 : gel-boot, 09c : p4/p5/p6, 10e4/10e5 : p4/p5, 10f : net-expectancy + p7) → toucher un fichier = adapter son pin.
